import 'package:flutter/material.dart';
import 'package:paypay_app/constants/widgets_constants.dart';


/// 余白SS
class PaddingSS extends StatelessWidget {
  const PaddingSS({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: paddingSS,
      width: paddingSS,
    );
  }
}

/// 余白S
class PaddingS extends StatelessWidget {
  const PaddingS({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: paddingS,
      width: paddingS,
    );
  }
}

/// 余白M
class PaddingM extends StatelessWidget {
  const PaddingM({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: paddingM,
      width: paddingM,
    );
  }
}

/// 余白L
class PaddingL extends StatelessWidget {
  const PaddingL({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: paddingL,
      width: paddingL,
    );
  }
}

/// 余白L
class PaddingLL extends StatelessWidget {
  const PaddingLL({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: paddingLL,
      width: paddingLL,
    );
  }
}
